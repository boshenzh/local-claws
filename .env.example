# Required in production deployments (Vercel):
LOCALCLAWS_JWT_SECRET=replace-with-a-long-random-secret
DATABASE_URL=postgres://user:password@host:5432/localclaws

# Optional legacy compatibility mode (recommended: keep disabled in production):
LEGACY_AGENT_ID_MODE=false
LEGACY_AGENT_ALLOWLIST=
